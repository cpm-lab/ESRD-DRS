# All of US (AoU) Analysis

To replicate the analyses in AoU, upload the scripts in this directory to your AoU workspace, as well as the model coeffiecients from the KDI/VHA analysis. Then run scripts 0-6 in order.

Analyses were performed within a workspace using the **Curated Data Repository (CDR) Version 8 (Controlled Tier)**.
