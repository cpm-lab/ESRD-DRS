# VHA analysis on KDI

This directory contains the code to run the analyses of VHA data within the U.S. Department of Energy (DOE) Knowledge Discovery Infrastructure (KDI) enclave.

To run the analysis, edit the paths in `config_mnt.R,` then run `submit_pipeline.sh`.
